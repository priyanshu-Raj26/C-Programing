#include <stdio.h>

int main() {
    int i = 0; // Initialize the variable

    while (i <= 20) {
        if (i >= 10) {
            printf("The value of i is %d\n", i);
        }
        i++; // Increment i
    }

    return 0;
}
