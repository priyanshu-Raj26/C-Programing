#include <stdio.h>

int maim()
{

    int a, b;
    printf("Enter length and breath of rectangle:");
    scanf("%d%d", &a, &b);
    printf("The area of rectangle is:", a * b);
    return 0;
}