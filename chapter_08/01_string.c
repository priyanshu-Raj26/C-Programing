#include <stdio.h>

int main() {
    char name[] = {'P', 'R', 'I', 'Y', 'A', 'N', 'S', 'H', 'U', '\0'};
    char class[] = "ishu";
    char address[] = "kendriya vidyalaya";

    
    printf("%s\n %s\n %s\n", name, class, address); // Use %s for strings

    return 0;
}
